// A config class for
// setting parameters of the simulation

class globals{
    constructor(){
        this.input_layer = 0; // starting layer
        this.output_layer = 10; // ending layer (should be more than input layer)
        this.inputs = 4; // number of inputs
        this.outputs = 2; // number of outputs
    }
}
